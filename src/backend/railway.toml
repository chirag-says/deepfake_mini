# Railway/Render Configuration for DeFraudAI Backend
# This file tells the platform how to build and run the backend

[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
# These should be set in the Railway/Render dashboard, not here
# JWT_SECRET_KEY = ""
# VITE_GEMINI_API_KEY = ""
# MONGODB_URI = ""
# FRONTEND_URL = ""
