# ===========================================
# DeFraudAI Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# REQUIRED - Security Critical
# ===========================================

# JWT Secret Key for authentication tokens
# Generate a strong random key: openssl rand -hex 32
# The application WILL NOT START without this
JWT_SECRET_KEY=your-256-bit-secret-key-here

# Gemini API Key (kept server-side only - NEVER exposed to frontend)
# Get from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your-gemini-api-key

# ===========================================
# CORS Configuration
# ===========================================

# Frontend URL(s) for CORS - comma separated for multiple origins
# Example: http://localhost:5173,https://yourdomain.com
FRONTEND_URL=http://localhost:5173

# ===========================================
# Database (Required for user auth & history)
# ===========================================

# MongoDB connection string
# Example: mongodb://localhost:27017 or mongodb+srv://user:pass@cluster.mongodb.net
MONGODB_URI=your-mongodb-connection-string

# MongoDB database name
MONGODB_DB_NAME=defraudai

# ===========================================
# Frontend Configuration (Vite)
# ===========================================

# Backend API URL for frontend to call
VITE_API_BASE_URL=http://localhost:8000

# Optional: Default Gemini model name
VITE_GEMINI_MODEL_NAME=gemini-2.5-flash

# ===========================================
# Optional - Third Party Services
# ===========================================

# Clerk authentication (if using Clerk instead of custom auth)
# VITE_CLERK_PUBLISHABLE_KEY=your-clerk-publishable-key

# SMTP for email notifications
# SMTP_API_KEY=your-smtp-api-key
